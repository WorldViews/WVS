import slumber
import json

api = slumber.API("https://localwiki.org/api/v4/maps/")

class mapPoints (object):

	points = False
def getMapPoints(self)
	if not self.points:
		
with open("Local_Wiki_Map_Points.json", "w") as writeJSON:
    json.dump(points, writeJSON)	

	
